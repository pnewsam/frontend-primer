---
import Container from "./Container.astro";
interface Props {
  // title: string;
  // body: string;
  // href: string;
}

// const { href, title, body } = Astro.props;
---

<Container class="max-w-screen-xl mx-auto px-4 py-4 border-b">
  <nav>
    <span>Frontend Primer</span>
  </nav>
</Container>
